<div class="flex flex-col items-center justify-center min-h-screen -m-8">  
  <h1 class ="-mt-5">
    <%= image_tag "/images/logo.png",alt: "Sketto", class:"w-64 md:w-80" %>
  </h1>

  <div class="cute-title">
    <div> メールアドレスの登録を行います。<br /></div>
    <div>ご利用のメールアドレスを入力してください。</div>
  </div>
 
  <%= form_with model: current_user, url: new_user_line_registration_path, method: :post do |f| %>
    <div>
      <%= f.label :email %><br>
      <%= f.email_field :email, required: true %>
    </div>

    <%= f.submit "登録する" %>
  <% end %>
</div>